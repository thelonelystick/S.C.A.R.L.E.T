# AI provider configuration will be added in Phase 5.
# Keep credentials in a local .env file; never expose them to the renderer.
AI_PROVIDER=
AI_API_KEY=

# Supabase browser client. These values are safe to expose in the renderer.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
